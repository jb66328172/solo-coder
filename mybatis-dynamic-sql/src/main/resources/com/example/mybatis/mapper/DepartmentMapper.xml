<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatis.mapper.DepartmentMapper">

    <resultMap id="DeptWithTeachersResultMap" type="Department">
        <id property="id" column="dept_id"/>
        <result property="name" column="dept_name"/>
        <result property="location" column="location"/>
        <collection property="teachers" ofType="Teacher">
            <id property="id" column="t_id"/>
            <result property="name" column="t_name"/>
            <result property="age" column="age"/>
            <result property="title" column="title"/>
            <result property="deptId" column="dept_id"/>
        </collection>
    </resultMap>

    <select id="selectDeptWithTeachersById" resultMap="DeptWithTeachersResultMap">
        SELECT d.id AS dept_id,
               d.name AS dept_name,
               d.location AS location,
               t.id AS t_id,
               t.name AS t_name,
               t.age AS age,
               t.title AS title
        FROM department d
        LEFT JOIN teacher t ON d.id = t.dept_id
        WHERE d.id = #{deptId}
    </select>

</mapper>
